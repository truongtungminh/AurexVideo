# AurexVideo (development)

Full source để phát triển sau này.
)
)
Structure:
- `server.py` local web API + static routes
- `core/generate_deck.py` programmatic deck generator
- `engine/` vendored FastScene engine source + assets
- `web/` surfaces: `/`, `/app`, `/watch.html`, `/settings`
- `assets/` fonts/logo/assets served at `/assets/`
- `decks/` generated decks live here
- `preview/` screenshots/captures

Status:
- Source copied from FastScene dump
- App routes implemented
- Assets mirrored
- Engine venv created
